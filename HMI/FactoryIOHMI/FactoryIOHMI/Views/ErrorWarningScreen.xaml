<UserControl x:Class="FactoryIOHMI.Views.ErrorWarningScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FactoryIOHMI.Views"
             xmlns:mod="clr-namespace:FactoryIOHMI.Models"
             xmlns:conv="clr-namespace:FactoryIOHMI.Converter"
             mc:Ignorable="d" 
             Background="Beige"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:BoolToStringConverter x:Key="BoolToStringConverter"
                                    TrueText="Aktiv"
                                    FalseText="Gegangen"/>
        <DropShadowEffect x:Key="ShadowNormal" BlurRadius="12" ShadowDepth="14" Opacity="0.35" />
        <DropShadowEffect x:Key="ShadowHover"  BlurRadius="16" ShadowDepth="16" Opacity="0.45" />
        <DropShadowEffect x:Key="ShadowPressed" BlurRadius="8" ShadowDepth="20" Opacity="0.20" />

        <Style x:Key="ShadowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Beige"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Face"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"
                            Effect="{StaticResource ShadowNormal}">

                            <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Face" Property="Effect" Value="{StaticResource ShadowHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Face" Property="Effect" Value="{StaticResource ShadowPressed}"/>
                                <Setter TargetName="Face" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Face" Property="Effect" Value="{x:Null}"/>
                                <Setter TargetName="Face" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="MinHeight" Value="50"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>
        <Style TargetType="Expander">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="IsExpanded" Value="False"/>
        </Style>
        <Style TargetType="Run">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.ColumnSpan="3" Grid.Row="0"
                 Margin="3"
                 HorizontalContentAlignment="Stretch"
                 ItemsSource="{Binding ErrorWarnings, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                 SelectedIndex="{Binding SelectedErrorWarning, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.Resources>
                <DataTemplate DataType="{x:Type mod:Error}">
                    <Expander Header="{Binding Message, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                  Background="Red">
                        <StackPanel Orientation="Vertical">
                            <TextBlock>
                                    <Run Text="Ist Aktiv: "/>
                                    <Run Text="{Binding IsActive, Mode=OneWay, UpdateSourceTrigger=PropertyChanged,
                                                Converter={StaticResource BoolToStringConverter}}"/>
                            </TextBlock>
                            <TextBlock>
                                    <Run Text="Zeitpunkt Aktivierung: "/>
                                    <Run Text="{Binding Activation, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}"/>
                            </TextBlock>
                        </StackPanel>
                    </Expander>
                </DataTemplate>
                <DataTemplate DataType="{x:Type mod:Warning}">
                    <Expander Header="{Binding Message, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                  Background="Yellow">
                        <StackPanel Orientation="Vertical">
                            <TextBlock>
                                    <Run Text="Ist Aktiv: "/>
                                    <Run Text="{Binding IsActive, Mode=OneWay, UpdateSourceTrigger=PropertyChanged,
                                                Converter={StaticResource BoolToStringConverter}}"/>
                            </TextBlock>
                            <TextBlock>
                                    <Run Text="Zeitpunkt Aktivierung: "/>
                                    <Run Text="{Binding Activation, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}"/>
                            </TextBlock>
                        </StackPanel>
                    </Expander>
                </DataTemplate>
            </ListBox.Resources>
        </ListBox>
        <Button Grid.Column="1" Grid.Row="1"
                Style="{StaticResource ShadowButtonStyle}"
                Content="Alle Quittieren"/>
        <Button Grid.Column="2" Grid.Row="1"
                Style="{StaticResource ShadowButtonStyle}"
                Content="Quittieren"/>
    </Grid>
</UserControl>
