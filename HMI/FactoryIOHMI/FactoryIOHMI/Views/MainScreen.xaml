<Window x:Class="FactoryIOHMI.Views.MainScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FactoryIOHMI.Views"
        xmlns:vm="clr-namespace:FactoryIOHMI.ViewModels"
        mc:Ignorable="d"
        Title="FactoryIO-HMI" Height="450" Width="800"
        WindowState="Maximized" WindowStartupLocation="CenterScreen"
        Background="Beige">
    <Window.Resources>
        <Style x:Key="BTNStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate DataType="{x:Type vm:HomeScreenViewModel}">
            <local:HomeScreen/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="150"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Grid.Column="0" Grid.Row="0" Source="/Images/Home.png" MaxHeight="100"
               HorizontalAlignment="Center"/>
        <GroupBox Grid.Column="1" Grid.Row="0" Margin="0"
                  FontWeight="Bold" FontSize="20"
                  Header="MachineState"
                  BorderBrush="Black" BorderThickness="2">
            <Grid>
                <Image Source="{Binding CurrentMachineState, 
                                        Mode=TwoWay, 
                                        UpdateSourceTrigger=PropertyChanged}" 
                       MaxHeight="200"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Stretch="Uniform"/>
                <Label HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="20" FontWeight="Bold" Foreground="Red"
                       Content="Produktion"/>
            </Grid>
        </GroupBox>
        <GroupBox Grid.Column="2" Grid.Row="0" Margin="0"
                  FontWeight="Bold" FontSize="20"
                  Header="Error"
                  BorderBrush="Black" BorderThickness="2">
                <Grid>
                    <Button Margin="5" Style="{StaticResource BTNStyle}" Background="Transparent">
                        <Image Source="/Images/Error.png" MaxHeight="100"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Stretch="Uniform"/>
                    </Button>
                </Grid>
        </GroupBox>
        <ContentControl Grid.ColumnSpan="3" Grid.Row="1"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                        Content="{Binding CurrentSubScreen,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</Window>
